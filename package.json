{
  "name": "pizzapos-server",
  "version": "1.0.0",
  "description": "PizzaPOS CTI backend — AMI listener + WebSocket server",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "asterisk-manager": "^0.1.9",
    "dotenv": "^16.4.5",
    "ws": "^8.18.0"
  }
}
